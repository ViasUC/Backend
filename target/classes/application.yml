spring:
  datasource:
    url: jdbc:postgresql://34.95.213.224:5432/postgres
    username: postgres
    password: UniversidadCatolica2025*
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: validate    # valida que las tablas ya existen, no las toca
    show-sql: true
    properties:
      hibernate:
        default_schema: public
        jdbc:
          lob:
            non_contextual_creation: true
    open-in-view: false

  graphql:
    schema:
      printer:
        enabled: true
